# 复制为 .env 后仅在本机填写；.env 已被 Git 忽略。以下全部是占位值。
MYSQL_ROOT_PASSWORD=change-me-mysql-root

NACOS_DB_USER=nacos
NACOS_DB_PASSWORD=change-me-nacos-db
NACOS_USERNAME=nacos
NACOS_PASSWORD=change-me-nacos
NACOS_NAMESPACE=docbase-dev
NACOS_GROUP=DOCBASE_GROUP
NACOS_AUTH_TOKEN=replace-with-base64-secret-at-least-32-bytes
NACOS_AUTH_IDENTITY_KEY=replace-with-server-identity-key
NACOS_AUTH_IDENTITY_VALUE=replace-with-server-identity-value

IAM_DB_USER=docbase_iam
IAM_DB_PASSWORD=change-me-iam-db
KNOWLEDGE_DB_USER=docbase_knowledge
KNOWLEDGE_DB_PASSWORD=change-me-knowledge-db
INGEST_DB_USER=docbase_ingest
INGEST_DB_PASSWORD=change-me-ingest-db
CHAT_DB_USER=docbase_chat
CHAT_DB_PASSWORD=change-me-chat-db
RAG_DB_USER=docbase_rag
RAG_DB_PASSWORD=change-me-rag-db

REDIS_ADMIN_PASSWORD=change-me-redis-admin
IAM_REDIS_PASSWORD=change-me-iam-redis
GATEWAY_REDIS_PASSWORD=change-me-gateway-redis
CHAT_REDIS_PASSWORD=change-me-chat-redis
KNOWLEDGE_REDIS_PASSWORD=change-me-knowledge-redis
INGEST_REDIS_PASSWORD=change-me-ingest-redis

RABBITMQ_USER=docbase
RABBITMQ_PASSWORD=change-me-rabbitmq
RABBITMQ_VHOST=/docbase

MINIO_ROOT_USER=docbase-minio-root
MINIO_ROOT_PASSWORD=change-me-minio-root
MINIO_APP_USER=docbase-app
MINIO_APP_PASSWORD=change-me-minio-app
MINIO_BUCKET=docbase-documents

# Knowledge Service multipart upload limits and the optional credential for the legacy
# metadata-only registration API. Browser clients must use /documents/upload instead.
KNOWLEDGE_DOCUMENT_MAX_FILE_SIZE=100MB
KNOWLEDGE_DOCUMENT_MAX_REQUEST_SIZE=101MB
KNOWLEDGE_DOCUMENT_UPLOAD_LEASE_DURATION=PT10M
KNOWLEDGE_INTERNAL_REGISTRATION_API_KEY=replace-with-private-internal-registration-key

# 不要从旧项目复制真实 Key 到仓库。模型名称继续沿用旧项目当前值。
DEEPSEEK_API_KEY=replace-with-local-secret
DEEPSEEK_MODEL=keep-existing-deepseek-model
RAG_INTERNAL_API_KEY=replace-with-local-internal-secret

# 后续 RAG 迁移时指向本机已有缓存；不要在本阶段下载新模型。
HF_HOME=

# IAM 非对称 JWT（RS256）。私钥仅由 iam-service 持有，公钥由 Gateway 和其他服务用于验签。
# 不要把真实私钥提交到仓库；生产环境应通过环境变量或 Docker Secrets 注入。
IAM_JWT_PRIVATE_KEY_PATH=.local/keys/docbase-iam-private.pem
JWT_PUBLIC_KEY_PATH=.local/keys/docbase-iam-public.pem
IAM_JWT_ISSUER=docbase-iam
IAM_JWT_ACCESS_TTL=30m
IAM_JWT_REFRESH_TTL=7d

# JWT auth_version 失败模式。
# fail-open (false, 默认): Redis 故障时允许 Token，保证高可用性。
# fail-closed (true): Redis 故障时拒绝 Token，保证高安全性。
JWT_FAIL_CLOSED=false

# 首次管理员网页初始化密钥（默认为空，即关闭）。初始化完成后再次清空并重建 IAM 服务。
IAM_ADMIN_SETUP_KEY=

# 可选的无人值守初始化：仅首次启动时开启，完成后立即改回 false 并清空密码。
IAM_INIT_ADMIN_ENABLED=false
IAM_INIT_ADMIN_USERNAME=admin
IAM_INIT_ADMIN_PASSWORD=

# Knowledge Outbox 发布配置
OUTBOX_POLL_INTERVAL_MS=5000
OUTBOX_POLL_BATCH_SIZE=10
OUTBOX_MAX_RETRIES=3
OUTBOX_RETRY_DELAYS=PT30S,PT5M,PT30M

# Ingest Service 配置
INGEST_POLL_INTERVAL_MS=5000
INGEST_RETRY_POLL_INTERVAL_MS=30000
INGEST_BATCH_SIZE=10
INGEST_MAX_RETRIES=3
INGEST_RETRY_DELAYS=PT30S,PT5M,PT30M

# RAG Service 配置
RAG_DATABASE_URL=mysql+pymysql://docbase_rag:change-me-rag-db@mysql:3306/docbase_rag
RABBITMQ_HOST=rabbitmq
RABBITMQ_PORT=5672
RABBITMQ_USER=docbase
RABBITMQ_PASSWORD=change-me-rabbitmq
RABBITMQ_VHOST=/docbase
MINIO_ENDPOINT=http://minio:9000
MINIO_RAG_ACCESS_KEY=docbase-rag
MINIO_RAG_SECRET_KEY=change-me-rag-minio
HF_HOME=
DEEPSEEK_API_KEY=replace-with-local-secret
RAG_INTERNAL_API_KEY=replace-me-internal-key

# Chat Service 配置
CHAT_STREAM_MAX_CONCURRENT=1
CHAT_STREAM_LOCK_TTL=120s
CHAT_STREAM_MAX_QUESTION_LENGTH=4000
CHAT_STREAM_MAX_RESPONSE_LENGTH=65536
CHAT_RAG_CONNECT_TIMEOUT=5s
CHAT_RAG_IDLE_TIMEOUT=120s
